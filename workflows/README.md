# workflows/

Optional deterministic orchestration scripts (for the Workflow tool) that the kit or a
project can run — e.g. a multi-agent "research → merge → build" pass when adding a new
ultra-skill. Empty for now; add scripts here as needed. See `docs/EXTENDING.md`.
