# Reusable sub-agent templates

Starting points the `new-project` flow specialises into a project's own
`<project>/.claude/agents/`. Don't ship these generic — copy, then bake in the project's real
names, paths, stack, and done-bar (see `skills/new-project/SUBAGENT_GENERATION.md`).
